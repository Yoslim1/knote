# التقرير الهندسي النهائي — Knote

**المؤلف:** Manus AI  
**التاريخ:** 23 أغسطس 2026  
**المستودع:** [Yoslim1/knote](https://github.com/Yoslim1/knote)  
**فرع العمل:** `modernize/ar-en-ui-and-rtl`  
**آخر commit:** `6a7b31f9ae01e6dd825af8116e616b7ac251d10a`

## 1. الملخص التنفيذي

تم فحص المستودع والتطبيق وطبقة البيانات وموارد اللغات والثيم ومسارات الأمان وملفات CI. نُفذت إعادة تصميم هادئة وحديثة مبنية على Material 3، مع توحيد الألوان والطباعة ومكونات التنقل والبطاقات، وإضافة دعم عربي مكتمل إلى جانب الإنجليزية مع fallback آمن. كما تم تطبيق اتجاه التخطيط الصحيح `RTL/LTR`، وتحديث تبديل اللغة ليعمل قبل فتح الواجهة وعلى Android 26–32 وكذلك الإصدارات الأحدث.

حافظت التغييرات على طبيعة Knote الأساسية: تطبيق Offline، وقاعدة Room فوق SQLCipher، وعدم حذف أي بيانات أو تغيير مخطط قاعدة البيانات دون ضرورة. لم أضف شبكة أو حسابات أو خدمة خارجية. التغييرات ركزت على تجربة الاستخدام، الموارد، السلامة، الاختبارات، ووثائق المشروع.

النتيجة الحالية قوية من ناحية البناء الآلي: تشغيل GitHub Actions الأخير رقم [32631332368](https://github.com/Yoslim1/knote/actions/runs/32631332368) انتهى بنجاح، وشمل فحص XML وتكافؤ اللغات، اختبارات الوحدة، Android Lint، تجميع APK debug، وتجميع مصادر اختبارات Android instrumented. أما تشغيل الاختبارات instrumented على محاكي Android فعلي فلم يُنفذ في هذه البيئة؛ لذلك يجب اعتباره خطوة تحقق لاحقة لا فشلًا مخفيًا.

## 2. نطاق الفحص

شمل الفحص الملفات والمناطق التالية:

| المجال | ما تم فحصه | النتيجة الهندسية |
|---|---|---|
| البنية | Gradle، Kotlin، Compose، Material 3، ViewModels، Repository | البنية الحالية قابلة للتطوير ولم تحتج إلى إعادة كتابة شاملة |
| الواجهة | `AppScaffold`، الأشرطة، التنقل السفلي، المهام، الملاحظات، المالية، العادات، التأمل، الحوارات | وُحدت الألوان والأشكال والتباعد وأسلوب البطاقات |
| اللغات | `values/strings_ui.xml` و`values-ar/strings_ui.xml` و`AppStrings` | أصبحت العربية مكتملة بمفاتيح وplural وarray مطابقة للإنجليزية |
| الاتجاه | الإقلاع، تبديل اللغة، Compose CompositionLocal، Android 26–32، Android 13+ | أضيف مسار مركزي لـ RTL/LTR وإعادة الإنشاء عند الحاجة |
| البيانات | Room، SQLCipher، AppDatabase، AppSettings، DAOs، مخططات قاعدة البيانات | لم يحدث تغيير مخطط أو حذف بيانات؛ أُبقي التوافق محفوظًا |
| الأمان | `SecurityController` و`SecuritySettings` ورسائل الأخطاء | أزيل عرض نصوص الاستثناء الخام واستُبدلت برسائل آمنة قابلة للترجمة |
| CI والإصدار | `ci.yml` و`release.yml` وversionCode وSemVer | أضيفت بوابات تحقق للغات والإصدار وتجميع AndroidTest |
| الوثائق | `README.md` و`ARCHITECTURE.md` وملفات التدقيق | حُدثت لتطابق الواقع الحالي بدل الوصف التاريخي |

## 3. ما تم تغييره

### 3.1 إعادة التصميم ونظام الهوية البصرية

أعيد بناء لوحة الألوان المركزية في `AppColors.kt` وربطت بالثيم الدلالي في `Theme.kt` بدل استخدام ألوان متفرقة داخل الشاشات. أصبحت الألوان مناسبة للوضع الفاتح والداكن، وأكثر هدوءًا واتساقًا مع هوية Knote. كما حُدثت مقاييس الطباعة في `Type.kt` لتحسين القراءة بالعربية والإنجليزية ورفع التدرج البصري للعناوين والنصوص.

استُبدل شريط التنقل السفلي اليدوي بمكوّن Material 3 حديث ومتجاوب، مع إزالة مضاعفة مساحة system bars التي كانت ممكنة بعد الانتقال إلى `NavigationBar`. كما حُسنت بطاقة المهمة، وألوان صفوف المالية والعادات والملاحظات، وشريط تنسيق الملاحظات، وحوار المصروفات، وعناوين الأقسام التي كانت تفرض `uppercase` غير مناسبة للعربية.

أصبحت شبكة الملاحظات متكيفة مع عرض الهاتف واللوحي بدل الاعتماد على كثافة ثابتة، مع الحفاظ على التمرير والوسوم وسلوك الملاحظات الحالي. لم تُحذف أي وظيفة مستخدم موجودة؛ التغيير بصري وتنظيمي مع إصلاحات توافقية.

### 3.2 دعم العربية والإنجليزية

أضيف ملف العربية الكامل `values-ar/strings_ui.xml`. نتيجة الفحص النهائي للموارد هي:

| نوع المورد | الإنجليزية | العربية | الناقص في العربية |
|---|---:|---:|---:|
| `string` | 781 | 781 | 0 |
| `plurals` | 16 | 16 | 0 |
| `string-array` | 4 | 4 | 0 |

أضيفت أداة `tools/audit_locales.py` لتجعل تكافؤ الموارد بوابة فشل في CI بدل أن تعود مشكلة نقص العربية بصمت. وأضيفت أداة `tools/translate_missing_arabic.py` للحفاظ على المفاتيح وplaceholders والتعددية والمصفوفات عند استكمال الترجمات مستقبلًا.

تمت معالجة plural العربية بصورة صحيحة بإضافة الفئات الست المطلوبة: `zero` و`one` و`two` و`few` و`many` و`other`. أضيفت أداة `tools/complete_arabic_plurals.py` لإعادة توليدها بشكل متسق. هذا أصلح خطأ AAPT ثم خطأ Android Lint الذي كان يطلب فئات عربية مفقودة.

### 3.3 سلوك تبديل اللغة والاتجاه

أضيف `AppLocalePreferences` كمدير مركزي صغير للغة. يدعم القيم التالية:

| القيمة | السلوك |
|---|---|
| `AUTO` | اتباع لغة النظام مع fallback إلى الإنجليزية عند عدم دعمها |
| `EN` | الإنجليزية واتجاه LTR |
| `AR` | العربية واتجاه RTL |

يتم حفظ اختيار اللغة في Room كما كان متوقعًا، وتُحفظ مرآة محلية صغيرة لتطبيق الاختيار مبكرًا قبل فتح الواجهة وقاعدة البيانات. أصبحت المرآة تُحدث بعد نجاح كتابة Room، لتجنب عدم تطابق المصدرين إذا فشلت الكتابة.

على Android 13+ يُستخدم `LocaleManager`، بينما تُستخدم على Android 26–32 آلية context محلي وإعادة إنشاء النشاط عند تغير اللغة حتى تتغير النصوص والحوارات النظامية أيضًا. يعتمد تحديد الاتجاه على `TextUtils.getLayoutDirectionFromLocale`، بينما يُمرر الاتجاه داخل Compose باستخدام `LocalLayoutDirection` من الحزمة الصحيحة `androidx.compose.ui.platform`. هذا يتوافق مع توثيق Android الرسمي الذي يوضح أن تغيير اتجاه Composable يتم عبر `LocalLayoutDirection` [1].

### 3.4 إصلاحات الأمان وتجربة الأخطاء

كانت بعض مسارات الأمان تعرض نص الاستثناء الخام للمستخدم، وهو غير مناسب من ناحية تجربة الاستخدام وقد يكشف تفاصيل تقنية أو رسائل غير مترجمة. أضيف معرّف رسالة عام وآمن إلى `SecurityStrings` وموارد اللغات، وأصبح `SecurityController` يعيد حالة قابلة للعرض بدل تمرير نص الاستثناء الخام إلى الواجهة.

لم تتغير خوارزميات التشفير أو مفاتيح SQLCipher أو Argon2 أو backup codec. هذا قرار مقصود لتجنب أي ترحيل أمني غير ضروري أثناء إعادة تصميم الواجهة.

### 3.5 تحسين CI والإصدار

أضيفت إلى CI بوابة تتحقق من صحة XML وتكافؤ موارد اللغات قبل Gradle. كما أضيفت خطوة `Compile Android instrumented tests` عبر `assembleDebugAndroidTest` للتأكد من ترجمة اختبارات قاعدة البيانات والتشفير واللغة، وليس الاكتفاء بتشغيل اختبارات الوحدة.

حُدث مسار الإصدار ليمنع تكرار `versionCode` أو إنشاء tag لا يطابق SemVer، مع إبقاء التشغيل اليدوي آمنًا عندما لا يكون هناك tag إصدار فعلي.

## 4. الأخطاء التي ظهرت وكيف عولجت

| الخطأ أو الدين التقني | السبب | المعالجة |
|---|---|---|
| `LocalLayoutDirection` غير معروف | استيراد من حزمة Compose غير صحيحة | نُقل الاستيراد إلى `androidx.compose.ui.platform` |
| `Locale.getLayoutDirection` و`LAYOUT_DIRECTION_RTL` غير موجودين | استعمال Java Locale API غير متاح لهذا الغرض | استُخدم `TextUtils.getLayoutDirectionFromLocale` و`View.LAYOUT_DIRECTION_RTL` |
| Lint: قراءة Configuration من `LocalContext` | قديمة ولا تعيد composition عند تغير Configuration | استُخدم `LocalConfiguration.current` |
| Kotlin: `configuration` خارج النطاق | استُخدمت قيمة محلية داخل `LaunchedEffect` قبل تعريفها | نُقلت إلى النطاق المشترك قبل التأثير |
| AAPT: quantity مكرر في plural عربي | مولد الموارد أنشأ quantity مكررًا | أُصلح المورد وأضيف تحقق XML/locale متكرر |
| Lint: فئات عربية مفقودة في 16 plural | العربية كانت تحتوي `one/other` فقط | أضيفت الفئات الست بصيغ عربية كاملة |
| ألوان تعتمد على أسماء ألمانية | منطق لون المالية كان يقارن نصوصًا محلية | رُبط اللون بهوية الفئة/الثابت بدل النص المعروض |
| نصائح التأمل تسقط إلى الإنجليزية | المحتوى الديناميكي لم يكن يختار العربية | أضيفت قائمة عربية وربطت باللغة الحالية |
| رسائل الأمان تعرض exception خامًا | تمرير خطأ تقني مباشرة إلى الواجهة | أضيف sentinel ورسالة عامة مترجمة |
| مساحة system bars مكررة | انتقال غير مكتمل إلى `NavigationBar` | أزيل padding السفلي المكرر |
| README يذكر تسع لغات ورابط مصدر قديم | وثائق تاريخية غير متزامنة مع fork الحالي | حُدثت اللغة والرابط والوصف |

## 5. طبقة البيانات: ما بقي وما لم يتغير

لم أضف migration إلى Room لأن التدقيق لم يثبت حاجة فعلية لتغيير schema، ولأن إضافة migration بلا فائدة مباشرة تزيد مخاطرة فقد البيانات. بقيت قاعدة البيانات المشفرة وتدفقات backup/restore والنماذج الحالية كما هي. لم تُحذف جداول أو أعمدة أو فهارس، ولم تُغير أرقام إصدارات Room أو تنسيق النسخ الاحتياطية.

| العنصر | القرار | السبب |
|---|---|---|
| Room schema | إبقاء schema الحالي | لا توجد حاجة وظيفية مثبتة، والحفاظ على التوافق أهم |
| SQLCipher | إبقاءه دون تعديل | حماية البيانات الحالية وعدم كسر فتح قواعد المستخدمين |
| Backup/restore | إبقاؤه دون تغيير | عدم المخاطرة بتوافق الملفات المشفرة القديمة |
| AppSettings | إضافة مرآة لغة فقط | تحسين الإقلاع والتوافق، دون نقل الإعدادات أو حذفها |
| DAO والفهارس | لم تُفرض تغييرات عشوائية | التحسين لا يبرر migration دون قياس أو استعلام متكرر واضح |
| حذف البيانات | لم يحدث | طلب المستخدم كان تحسينًا مع الحفاظ على البيانات |

## 6. ما أُضيف وما لم يُحذف

أُضيفت ملفات مدير اللغة، اختبار اللغة الآلي، أدوات تدقيق XML والموارد، أداة إكمال plural العربية، وملفات توثيق وخطة التنفيذ والتدقيق. كما أضيفت ترجمات عربية ومحتوى تأمل عربي ورسائل أمان مترجمة.

لم يُحذف أي ملف من وظائف التطبيق أو طبقة البيانات. الملفات المؤقتة مثل سجلات CI وملفات التحقق المحلية أزيلت من مساحة العمل ولم تُضمّن في الفرع. لم تُحذف قاعدة بيانات أو schema أو اختبار موجود. التغييرات التي تبدو حذفًا في diff هي في الغالب استبدال مكونات واجهة قديمة بمكونات Material 3 أو إزالة قيمة لون ثابتة، وليست إزالة وظيفة.

## 7. الاختبارات ونتائجها

| الاختبار | النتيجة | الملاحظات |
|---|---|---|
| `tools/validate_xml.py` | ناجح | تم التحقق من 41 ملف XML |
| `tools/audit_locales.py` | ناجح | 781 string و16 plural و4 arrays، دون نقص |
| `git diff --check` | ناجح | لا توجد whitespace errors |
| `:app:testDebugUnitTest` في CI | ناجح | شمل اختبارات الأمان والبيانات والواجهة الموجودة |
| `:app:lintDebug` في CI | ناجح | مر بعد إصلاح imports وConfiguration وArabic plural |
| `:app:assembleDebug` في CI | ناجح | تم تجميع APK debug |
| `:app:assembleDebugAndroidTest` في CI | ناجح | تم تجميع مصادر Android instrumented tests |
| اختبارات `AppLocalePreferencesInstrumentedTest` | مترجمة ضمن AndroidTest | لم تُشغّل على emulator في هذه البيئة |
| Gradle محليًا | غير قابل للتنفيذ في sandbox | البيئة المحلية لا تحتوي Android SDK/`ANDROID_HOME` |

تم الوصول إلى نجاح CI في التشغيل الأخير [32631332368](https://github.com/Yoslim1/knote/actions/runs/32631332368) على commit `6a7b31f`. فشل التشغيلات الوسيطة لم يُخفَ؛ تم تشخيص كل فشل وإصلاحه، ومنها import Compose، scope Configuration، وplural العربية.

## 8. ما بقي كتحقق لاحق أو تحسين اختياري

الخطوة الوحيدة غير المنفذة داخل هذه البيئة هي تشغيل Android instrumented tests على emulator أو جهاز فعلي. تجميعها نجح، لكن التشغيل يتطلب Android SDK ومحاكيًا. يوصى قبل الإصدار النهائي بتشغيل:

```sh
./gradlew :app:connectedDebugAndroidTest
```

كما يوصى بإجراء مراجعة بصرية على جهاز صغير وجهاز لوحي لكل من العربية والإنجليزية، في الوضعين الفاتح والداكن، مع اختبار تدوير الشاشة وتكبير الخط وتغيير اللغة أثناء فتح الحوار. هذه ليست فجوة في البناء، بل تحقق واجهة فعلي يحتاج جهازًا أو emulator.

يمكن لاحقًا إضافة screenshot tests وbaseline بصري للشاشات الرئيسية، لكنني لم أضفها الآن حتى لا أثبت صورًا مرتبطة بمحاكي واحد أو أخلط تحسين التصميم مع تغيير سلوك التطبيق. ويمكن لاحقًا إضافة لغة ثالثة عبر نفس مسار `audit_locales.py` دون إعادة بناء طبقة البيانات.

## 9. طريقة مراجعة وتشغيل الفرع

```sh
git clone https://github.com/Yoslim1/knote.git
cd knote
git checkout modernize/ar-en-ui-and-rtl
./gradlew :app:testDebugUnitTest
./gradlew :app:lintDebug
./gradlew :app:assembleDebug
./gradlew :app:assembleDebugAndroidTest
```

الفرع المرفوع هو `modernize/ar-en-ui-and-rtl`، بينما بقي `main` دون تعديل مباشر. مجموع الفرق عن `origin/main` هو 36 ملفًا، مع 1708 إضافة و185 حذفًا؛ ويشمل ذلك الكود والموارد والوثائق وأدوات التحقق.

## 10. سجل commits الرئيسي

| Commit | الغرض |
|---|---|
| `695eff4` | إعادة تصميم أولية ودعم العربية وRTL |
| `389278e` | إصلاح quantities العربية المتعارضة |
| `a8d91ef` | تصحيح APIs الخاصة بـ Compose RTL |
| `3acc9d3` | معالجة lint الخاص بقراءة Configuration |
| `3c00817` | تصحيح نطاق Configuration في Kotlin |
| `f92297c` | تثبيت المسار النهائي لتحديث اللغة |
| `4c5203e` | إكمال فئات plural العربية وإضافة الأداة القابلة لإعادة الاستخدام |
| `6a7b31f` | إضافة تجميع Android instrumented tests إلى CI |

## 11. الحكم النهائي

الفرع جاهز للمراجعة البرمجية وفتح Pull Request، ومناسب كمرشح دمج بعد تشغيل الاختبارات instrumented على emulator أو جهاز فعلي. لا توجد تغييرات مدمرة لبيانات المستخدم، ولا يوجد حذف لوظائف التطبيق، ولا تمت إضافة شبكة أو تتبع أو حسابات. أكبر التحسينات المرئية هي اتساق Material 3، وضوح الوضع الداكن، تكيف شبكة الملاحظات، اكتمال العربية، وسلامة تبديل الاتجاه واللغة عبر إصدارات Android المدعومة.

### المراجع

[1]: https://developer.android.com/develop/ui/compose/layouts/custom "Android Developers — Custom layouts and LocalLayoutDirection"
[2]: https://github.com/Yoslim1/knote/actions/runs/32631332368 "Knote CI run 32631332368"
